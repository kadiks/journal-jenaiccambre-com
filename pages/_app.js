import App from "next/app";
import Router from "next/router";

import * as gtag from "../src/components/analytics";

// Router.events.on("routeChangeComplete", url => {
//   console.log("_app.js url", url);
//   gtag.pageview(url);
// });

export default App;
